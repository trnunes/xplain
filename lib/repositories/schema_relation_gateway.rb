module Xplain
    class SchemaRelationGateway
        
        def image(relation, offset = 0, limit = -1, crossed=false, &block)
        end
        
        def domain(relation, offset=0, limit=-1, crossed=false, &block)
        end

    end

end

