module RefineHelper
  
  #TODO facets has to come from the exploration model since we can apply operations over each such as order facets
  def facets_for(groups)
    
  end
  
  def values_for(groups, facet)
  end
  
  def facet_size(groups, facet, value)
    
  end
end
