var count = 0;
var common_relations = [
	"ID",
	<%@common_relations.each do |relation|%>

		"<%=relation%>",

	<%end%>
]
var common_relations_menu = {};
for (var i=0; i < common_relations.length; i++) {
	common_relations_menu[i] = common_relations[i];
}
